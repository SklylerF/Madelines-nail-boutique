<section class="container">
  <section class="flex-row justify-center">
    {{#each galleries as |painting|}}
    <section class="card">
    <div class="card-body">
      <img src="/images/{{painting.filename}}">
    </div>
    </section>
    {{/each}}
  </section>
</section>
